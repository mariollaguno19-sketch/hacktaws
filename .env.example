# ── Google Gemini ──
# Clave de Google AI Studio (https://aistudio.google.com/apikey)
GEMINI_API_KEY="tu_api_key_aqui"

# Pon "true" para probar sin API key de Gemini (respuestas simuladas)
MOCK_GEMINI="false"

# Pon "true" para probar sin Supabase (base de datos simulada en memoria; usuario admin/admin)
MOCK_DATABASE="false"

# ── Supabase (proyecto: hacktaws) ──
SUPABASE_URL="https://utwkppgkdircotkhmntf.supabase.co"

# Clave SECRETA (service_role / sb_secret). NUNCA la expongas en el frontend ni la subas a git.
# Cópiala desde: https://supabase.com/dashboard/project/utwkppgkdircotkhmntf/settings/api-keys
SUPABASE_SECRET_KEY="tu_clave_secreta_aqui"

# ── Servidor ──
PORT=3000

# ── Límites de peticiones por IP (por minuto). Vacío = usa los defaults de producción ──
# RL_GLOBAL=120   # techo general de /api
# RL_CHAT=20      # /api/chat
# RL_LOGIN=5      # /api/auth/login
# RL_EVALUAR=5    # /api/evaluate
